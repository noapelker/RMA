import React from 'react';
import Home from "./Home";

const Main = props => {
    return (
        <div className={'mainContainer'}>
            <Home/>
        </div>
    );
};

export default Main;